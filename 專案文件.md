# 影音串流平台模擬系統 - 技術開發文件

## 1. 專案概述
本專案實作一個具備商用邏輯的影音串流後端模擬器。核心功能聚焦於「自動化權限驗證」與「播放會話生命週期管理」，模擬真實平台對不同層級用戶的內容派發邏輯。

## 2. 系統架構設計 (System Architecture)
本系統採用物件導向設計 (OOP)，落實「單一職責原則」，將資料實體與業務邏輯解耦：

### 核心模組職責
* **Video (資料實體)**：封裝影片元數據，包含標題、分類、分級(Rating)、授權地區(Allowed Regions)及付費屬性(isPremium)。
* **VideoPermissionService (核心驗證)**：系統守門員，負責執行年齡檢查、地區合規性驗證、訂閱方案比對及併發裝置數限制。
* **PlaybackSession (會話管理)**：負責執行播放期間的特定行為，如針對免費用戶的廣告插入邏輯。
* **WatchHistoryService (紀錄與推薦)**：維護觀看紀錄軌跡，並提供基於分類偏好的演算法推薦。



## 3. 關鍵技術實作
### A. 異常驅動的權限驗證
系統採用 `Exception` 拋出機制替代傳統的布林值回傳。
- **技術優點**：確保 `Main` 主程式必須強制處理存取失敗的情況，並能傳遞具體的錯誤原因（如：年齡未達標或地區未授權），提升系統 Robustness。

### B. 封裝與策略應用
透過 `PlaybackSession` 類別，將「播放前導邏輯」與「主程式流程」分離。系統會根據 `User.plan` 的屬性，自動切換是否顯示廣告提示。

## 4. 開發挑戰與對策
- **資料完整性**：修正建構子參數排序問題，確保 `Video` 物件初始化時的資料準確性。
- **邏輯模組化**：將原本臃腫的 `Main` 程式碼拆分至專屬 Service，提升代碼的可讀性與後續維護彈性。